language: python

python:
         - 2.7

before_install:
        - pip install -r requirements.txt --exists-action w --upgrade
        - pip install pytest pylint pytest-factoryboy

script: pylint --errors-only tests/ utils.py config.py toaster_profile.py
